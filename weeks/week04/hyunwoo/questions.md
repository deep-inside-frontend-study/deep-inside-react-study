- 자식 컴포넌트에서 새 컴포넌트를 매번 렌더링하는 방식과, 부모에서 자식 컴포넌트 사이에 Children 컴포넌트를 배치하고 props를 넘기는 방식이 나옵니다. 결국 children으로 받는 입장에선 굳이 내가 자식으로 또 props를 넘겨줄 필요가 없다는 이야기인것으로 이해했는데, 예시 자체가 좀 이해가 잘 안 갔습니다. 이 부분 다들 이해가 잘 되셨는지 궁금합니다.
- 우선순위를 의도적으로 낮추더라도, setState 배칭 작업에 포함되고, 한 번의 커밋 내역에 포함되는 게 맞겠죠? 더블 버퍼링 관련 내용으로 혼동이 좀 있는데, 더블 버퍼링에서는 렌더링 작업을 WIP트리에서 완료 후에 current 트리로 바꿔치기한다고 합니다. state 변화가 있고, 이 state를 사용하는 컴포넌트들이 여러 곳이라면 1곳만 업데이트 된 상태를 보여주지 않는다 라고 이해했는데 맞을까요? 깨진 UI 기준을 잘 모르겠습니다. 다들 어떻게 이해하셨는지 궁금합니다.
